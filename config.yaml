# Z-Hermes 配置文件
# 修改后重启程序生效

model:
  api_key: "lm-studio"
  base_url: "http://localhost:1234/v1"
  model: "qwen3.5-35b-a3b"
  max_tokens: 400

agent:
  max_iterations: 15

learning:
  memory_nudge_interval: 5
  skill_nudge_interval: 8

aux_model:
  max_tokens: 300
